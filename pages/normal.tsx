import { FC } from "react";

const Normal: FC = () => {
  return (
    <div>
      <p>
        Take a look at the head to see what has been added, you are looking for
        a script tag of type
      </p>
    </div>
  );
};

export default Normal;
